services:
  nlp-api:
    image: ${IMAGE_URL}
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nlp-api-server
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: always
